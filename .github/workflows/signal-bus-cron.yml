name: Signal Bus Consumer Cron

on:
  # 禁用自动运行，只能手动触发
  workflow_dispatch:
  # schedule:
  #   - cron: '*/10 * * * *'

jobs:
  trigger-signal-bus-consumer:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger signal-bus-consumer Edge Function
        run: |
          echo "This workflow requires SIGNAL_BUS_SECRET to be configured"
          echo "Please set it in: Settings → Secrets and variables → Actions"
          echo ""
          echo "待配置信息："
          echo "1. SUPABASE_URL - 你的 Supabase 项目 URL"
          echo "2. SIGNAL_BUS_SECRET - signal-bus-consumer 的密钥"
